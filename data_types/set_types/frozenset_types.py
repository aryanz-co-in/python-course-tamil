# set, frozenset are  Set Types

# Does not maintain the sorting order - it gets randomize

states = frozenset({"Kerala", "TamilNadu", "Karnataka", "Assam", "Assam"})
print(states)

num = frozenset((20, 23, 11, 20))
print(num)
